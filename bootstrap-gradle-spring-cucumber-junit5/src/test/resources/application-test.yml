server:
    port: 9080
    servlet:
        context-path: /api

logging:
    level:
        org.springframework: ERROR
        liquibase: ERROR
        org.hibernate: ERROR
        io.tricefal.core: DEBUG
    pattern:
        console: "%d{HH:mm:ss.SSS} [%t] %-5level %logger{36} - %msg%n"

