import { Sample } from './sample'

describe('Playing ', () => {
  it('should return xxx when yyy', () => {
    const input = ''
    const output = 0

    const result = Sample(input)

    expect(result).toEqual(output)
  })

})
